# ==========================================
# PetFinder — Python Dependencies
# ติดตั้ง: pip install -r requirements.txt
# ==========================================

# Django core
Django>=4.2,<5.1
python-dotenv>=1.0.0

# Database (PostgreSQL + pgvector)
psycopg2-binary>=2.9.9
pgvector>=0.2.5

# Supabase Storage (S3-compatible)
django-storages>=1.14.2
boto3>=1.34.0

# Image processing & AI (ฟรีทั้งหมด)
Pillow>=10.0.0
# CPU-only PyTorch (เล็กลง ~5x จาก default ~2GB → ~200MB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0

# Production server + static files
gunicorn>=21.2.0
whitenoise>=6.6.0
dj-database-url>=2.1.0

# Auth helper — decode Supabase JWT
PyJWT>=2.8.0

# HTTP client (ใช้ verify JWT + future API calls)
requests>=2.31.0

# Price scraper (ดึงราคาจาก external_link ของสินค้า)
beautifulsoup4>=4.12.0
